#include <stdio.h>

/**
 * main - prints name, followed by a new line.
 * @argc: argument count
 * @argv: array of arguments
 * code by sue
 * Return: 0
 */
int main(int argc, char **argv)
{
	(void)argc;

	printf("%s\n", argv[0]);
	return (0);
}
